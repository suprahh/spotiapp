import { ImagesUrlPipe } from './images-url.pipe';

describe('ImagesUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagesUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
